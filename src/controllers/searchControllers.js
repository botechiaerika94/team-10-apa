const { search } = require("../routes/productsRoutes")

let controller = {
        search: (req, res) => {
                router.get('/', (req, res) => {
                            res.send('pagina  search en construccion')
                        }
                        searchP: (req, res) => {
                            let busquedaP = {
                                {
                                    id:
                                }

                            }
                        }